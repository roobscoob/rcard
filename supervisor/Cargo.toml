[package]
name = "supervisor"
version = "0.1.0"
edition = "2021"

[dependencies]
userlib = { workspace = true, features = ["no-panic-messages"] }
kipc.workspace = true

[[bin]]
name = "supervisor"
test = false
bench = false
