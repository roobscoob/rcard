[workspace]

[package]
name = "dump-places"
version = "0.1.0"
edition = "2024"

[dependencies]
rcard-places = { path = "../../../shared/places", features = ["std"] }
zip = { version = "2", default-features = false, features = ["deflate"] }
