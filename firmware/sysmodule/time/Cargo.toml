[package]
name = "sysmodule_time"
version = "0.1.0"
edition = "2021"

[features]
default = ["bare-panics"]
bare-panics = ["ipc/bare-panics"]

[dependencies]
ipc = { path = "../../modules/ipc" }
generated = { path = "../../modules/generated" }
sysmodule_time_api = { path = "api", features = ["bare-panics"] }
userlib.workspace = true

[[bin]]
name = "sysmodule_time"
test = false
bench = false

[lints]
workspace = true
