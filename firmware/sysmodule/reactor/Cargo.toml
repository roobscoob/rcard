[package]
name = "sysmodule_reactor"
version = "0.1.0"
edition = "2021"

[features]
default = ["bare-panics"]
bare-panics = []

[dependencies]
idyll_runtime.workspace = true
ipc = { path = "../../modules/ipc" }
heapless = "0.8"
sysmodule_reactor_api = { path = "api", features = ["bare-panics"] }
userlib.workspace = true

[build-dependencies]
serde_json = "1"

[[bin]]
name = "sysmodule_reactor"
test = false
bench = false

[lints]
workspace = true
