[package]
name = "sysmodule_sdmmc_api"
version = "0.1.0"
edition = "2021"

[features]
bare-panics = []
host = ["ipc/host"]

[dependencies]
ipc = { path = "../../../modules/ipc" }
rcard-log = { path = "../../../../shared/log" }
zerocopy = "0.8"
storage_api = { path = "../../../modules/storage_api" }

[target.'cfg(target_os = "none")'.dependencies]
userlib.workspace = true

[lib]
test = false
bench = false

[lints]
workspace = true
